[package]
name = "shared_core"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }
uuid = { workspace = true }
rust_decimal = "1.35.0"
rust_decimal_macros = "1.35.0"
serde = { version = "1.0.228", features = ["derive"] }
